# 冰島 2026 行程手冊 - 版本變更紀錄 (Version Log)

本文檔記錄《冰島 2026 夢幻之旅》網頁手冊的所有版本標籤與重大更新說明。若未來需要回復至特定版本，可參考對應的 Git Tag 標籤。

---

## [v1.4] - 2026-08-28 (當前最新穩定版)
**Git Tag**: `v1.4`  
**主要更新簡介**:
1. **📌 行程頁面精簡 (`index.html`)**：
   - 精準移除「目前版本先保留的疑問」區塊，介面更乾淨俐落。
2. **✈️ 交通票個人獨立儲存 (`transport.html`)**：
   - 交通票改為每位成員獨立儲存，所有人皆可點擊「✏️ 編輯」編輯自己的班機與車票。
3. **🏠 住宿預設與雲端連動 (`accommodation.html`)**：
   - 預載 3 家飯店細節與入住日期，全隊登入皆可檢視；管理者編輯即時同步雲端。
4. **✅ 準備與打包清單 CRUD (`checklist.html`)**：
   - 解鎖細項編輯權限，成員預設去除無關項目，支援「＋ 新增大分類卡片」與標題編輯/卡片刪除。

---

## [v1.3] - 2026-08-28

**Git Tag**: `v1.3`  
**主要更新簡介**:
1. **🛍️ 伴手禮頁面增強 (`shopping.html`)**：
   - 新增「＋ 新增個人自訂必買」功能，支援輸入自訂商品名稱。
   - 每個卡片新增刪除（`×`）按鈕，可隨時刪除自訂項目，資料寫入個人暫存。
2. **✈️ 交通票全面可編輯 (`transport.html`)**：
   - 去程航班、租車專案、回程航班卡片全數新增「✏️ 編輯」功能。
   - 支援修改起終點、航班號/車型、訂單號、時間與備註說明。
3. **🏠 住宿日期選擇 Bug 解鎖 (`accommodation.html`)**：
   - 移除原先硬性限制 2026-09-23 ~ 2026-10-03 的區間檢查與彈窗。
   - 現在支援自由新增任意日期至各住宿點，不會再出現「找不到日期在行程中」的卡住錯誤。

---

## [v1.2] - 2026-08-28

**Git Tag**: `v1.2`  
**主要更新簡介**:
1. **🔐 Firebase Authentication (Google 帳號登入)**：
   - 導入 Firebase 安全驗證，全站頁面訪問需進行 Google 帳號登入驗證。
2. **⏳ 管理者白名單審核機制 (Admin Approval Whitelist)**：
   - 登入後若未獲管理者同意，顯示「等待開通權限」遮罩保護頁面內容。
   - 最高管理者 (`dreamland11023@gmail.com`) 可在前台 Topbar 打開「⚙️ 成員審核」面板一鍵【同意開通】或【移除權限】。
3. **🛡️ 模式 A 權限控管 (Mode A RBAC)**：
   - 只有管理者帳號能夠開啟「✏️ 編輯模式」修改行程、景點與住宿。
   - 授權旅伴享有全站檢視權限與個人資料編輯權限。
4. **☁️ Cloud Firestore 雲端資料同步**：
   - 共用行程與住宿資料即時同步至雲端，且支援管理者遠端回復。

---

## [v1.1] - 2026-08-28

**Git Tag**: `v1.1`  
**主要更新簡介**:
1. **🏠 住宿資訊獨立頁面 (`accommodation.html`)**：
   - 提供 3 大住宿地點卡片、迷你地圖與全島分布圖。
   - 支援卡片行內編輯（飯店名稱、Check-in/out 時間、訂單號、電話、備註）。
   - 支援日曆選擇器新增/刪除入住日期。
2. **📅 每日行程住宿選擇器**：
   - 編輯模式下，每日行程卡片新增住宿下拉選單，選取後與住宿管理頁面完全同步存取。
3. **✈️ 交通票獨立頁面 (`transport.html`)**：
   - 登機證樣式時間軸，包含班機資訊、租車車型、歸還資訊等框架。
4. **🛋️ 伴手禮/必買清單頁面 (`shopping.html`)**：
   - 12 項精選冰島必買商品/店家卡片，支援類別過濾、完成率統計、與每日行程錨點連結。
5. **✅ 準備與打包二合一清單頁面 (`checklist.html`)**：
   - 合併「行前準備」與「行李打包」至獨立 Tab 頁面。
   - 編輯模式下支援項目動態新增（Enter 鍵）、內文修改與刪除（`×`）功能。
6. **🔗 導覽列全頁面同步**：
   - 所有分頁 Topbar 及手機版選單均已更新至最新結構。

---

## [v1.0] - 2026-08-14
**Git Tag**: `v1.0`  
**主要更新簡介**:
- 完成經典 11 日冰島環島行程手冊基底。
- 包含今日模式、全程互動地圖 (Leaflet)、每日景點細節與裝備建議。
- 支援深色模式 (Dark Mode) 與手動編輯模式。
