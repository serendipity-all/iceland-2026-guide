# 冰島 2026 行程手冊 - 版本變更紀錄 (Version Log)

本文檔記錄《冰島 2026 夢幻之旅》網頁手冊的所有版本標籤與重大更新說明。若未來需要回復至特定版本，可參考對應的 Git Tag 標籤。

---

## [v1.2] - 2026-09-01 (當前最新穩定版)
**Git Tag**: `v1.2`  
**主要更新簡介**:
1. **✈️ 交通頁面完整重寫 (`transport.html`)**：
   - localStorage 個人化儲存（Key: `iceland_transport_v12`），所有人資料獨立不互干擾。
   - 登機證卡片改為 **3 格版面**：車型/類別、出發（日期+時間）、抵達（日期+時間）。
   - 支援出發/抵達各自獨立選日期+時間（含跨日支援）。
   - 行內編輯表單包含 11 欄位：類別、起站、出發日期/時間、目的站、抵達日期/時間、航班號/車型、座位/保險、訂單號、備註。
   - ➕ **手動新增交通項目**（新增後自動展開編輯區與滾動）。
   - 🗑️ **手動刪除交通項目**（帶確認提示）。
   - ⬆⬇ **手動調整卡片順序**（移上/移下按鈕）。
2. **🏠 住宿頁面補強 (`accommodation.html`)**：
   - 新增「🔄 回復預設」按鈕，可一鍵清除個人住宿資訊並恢復預設三家住宿。
3. **✅ 清單頁面補強 (`checklist.html`)**：
   - 編輯模式下每個 Tab 新增「＋ 新增類別」按鈕，可動態新增自訂分類卡片。
   - 各類別卡片的編輯區新增「🗑️ 刪除此類別」按鈕（含確認提示）。
   - 類別設定儲存於 `iceland_checklist_cats_v12`，個人自訂類別跨重整持久保存。

---

## [v1.1] - 2026-08-28
**Git Tag**: `v1.1`  
**主要更新簡介**:
1. **🏠 住宿資訊獨立頁面 (`accommodation.html`)**：
   - 提供 3 大住宿地點卡片、迷你地圖與全島分布圖。
   - 支援卡片行內編輯（飯店名稱、Check-in/out 時間、訂單號、電話、備註）。
   - 支援日曆選擇器新增/刪除入住日期。
2. **📅 每日行程住宿選擇器**：
   - 編輯模式下，每日行程卡片新增住宿下拉選單，選取後與住宿管理頁面完全同步存取。
3. **✈️ 交通票獨立頁面 (`transport.html`)**：
   - 登機證樣式時間軸，包含班機資訊、租車車型、歸還資訊等框架。
4. **🛋️ 伴手禮/必買清單頁面 (`shopping.html`)**：
   - 12 項精選冰島必買商品/店家卡片，支援類別過濾、完成率統計、與每日行程錨點連結。
5. **✅ 準備與打包二合一清單頁面 (`checklist.html`)**：
   - 合併「行前準備」與「行李打包」至獨立 Tab 頁面。
   - 編輯模式下支援項目動態新增（Enter 鍵）、內文修改與刪除（`×`）功能。
6. **🔗 導覽列全頁面同步**：
   - 所有分頁 Topbar 及手機版選單均已更新至最新結構。

---

## [v1.0] - 2026-08-14
**Git Tag**: `v1.0`  
**主要更新簡介**:
- 完成經典 11 日冰島環島行程手冊基底。
- 包含今日模式、全程互動地圖 (Leaflet)、每日景點細節與裝備建議。
- 支援深色模式 (Dark Mode) 與手動編輯模式。
